{
  "name": "EJaxMediaSystem",
  "version": "3.0.0-rc1",
  "description": "This program simplifies weekly music mangment.",
  "main": "main.mjs",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-x86-win": "electron-packager . app --overwrite --platform win32 --arch x64 --out dist/",
    "build-x86-linux": "electron-packager . app --overwrite --platform linux --arch x64 --out dist/"
  },
  "author": "Christian Lockley",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@electron/packager": "^18.3.2",
    "electron": "^31.0.0"
  },
  "dependencies": {
    "atem-connection": "^3.5.0",
    "bonjour": "^3.3.0",
    "electron-settings": "^4.0.4",
    "hls.js": "^1.1.5"
  }
}
